package options

const (
	InvalidCommand       = "invalid command try help"
	DriverIsNotSpecified = "driver not specified"
	InvalidDriver        = "invalid driver"
	HelpCommand          = "Help command"
)
